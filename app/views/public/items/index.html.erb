商品一覧

<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image(100,100) %>
    <% end %>
    <p>商品名：<%= item.name %></p>
    <P>税込価格 <%= item.add_tax_price.to_s(:dalimited) %></p>
  </div>

<% end %>

<%= paginate @items %>
