商品一覧

<% @items.each do |item| %>
  <div>
    <%= image_tag item.get_image(100,100) %>
    <p>商品名：<%= item.name %></p>
    <P>税込価格 <%= item.add_tax_price.to_s(:dalimited) %></p>
  </div>

<% end %>

<%= paginate @items %>
