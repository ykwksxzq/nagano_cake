<h2>商品一覧</h2>

<%= link_to "+", new_admin_item_path %>

<% @items.each do |item| %>
  <div>
    商品ID  <%= item.id %>
    商品名　<%= link_to item.name, admin_item_path(item.id) %>
    税込価格 <%= item.price %>
    販売ステータス
  </div>

<% end %>

<%= paginate @items %>
